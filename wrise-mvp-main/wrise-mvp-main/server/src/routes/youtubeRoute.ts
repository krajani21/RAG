// youtubeRoute.ts
// import express, { RequestHandler } from "express";
// import {
//   fetchUserVideos,
//   fetchComments,
//   fetchTranscripts,
//   fetchAndStoreYouTubeData
// } from "../controllers/youtubeController";

// const router = express.Router();

// router.get("/videos", fetchUserVideos as RequestHandler);
// router.get("/comments/:videoId", fetchComments as RequestHandler);
// router.get("/transcripts/:videoId", fetchTranscripts as RequestHandler);
// router.get('/store', fetchAndStoreYouTubeData as RequestHandler);


// export default router;
